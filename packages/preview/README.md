# @appcraft-io/preview

AppCraft instant HTML preview renderer for `app.acm.yaml` models — a deterministic,
self-contained mockup with no Android toolchain required.

Part of [AppCraft](https://github.com/terminalis/appcraft) — the deterministic app compiler for the AI era.
